schema = 1

[unit]
name = "recorder"
kind = "service"
description = "A recorder that is up but holds every query for a while before answering (#102)"

[runtime]
command = "uv run units/recorder.py"
restart = "always"
shutdown_grace_s = 5

[bus.publishes]
history = { key = "home/history/**" }
