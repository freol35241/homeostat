schema = 1

[entity]
# id is the bridge's MQTT_BASE_TOPIC; deliberately not the entity name, so
# the tests prove binding goes through the id.
id = "aduro2mqtt"
capability = "burner"
features = ["power_level"]
room = "livingroom"

[write_policy]
mode = "arbitrated"
owner = "aduro"
