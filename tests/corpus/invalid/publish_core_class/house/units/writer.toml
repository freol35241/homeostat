schema = 1

[unit]
name = "writer"
kind = "automation"

[runtime]
command = "uv run units/writer.py"
restart = "on-failure"

[bus.publishes]
config = { key = "home/config/zigbee/poll_s" }
meta = { key = "home/meta/zigbee/manifest_hash" }
