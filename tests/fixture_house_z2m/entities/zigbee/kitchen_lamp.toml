schema = 1

[entity]
# id is the Zigbee2MQTT topic segment (friendly name); deliberately not the
# entity name, so the tests prove binding goes through the id.
id = "lamp_kitchen_1"
capability = "light"
features = ["brightness"]
room = "kitchen"

[write_policy]
mode = "shared"
owner = "zigbee"
