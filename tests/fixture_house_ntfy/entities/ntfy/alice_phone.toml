schema = 1

[entity]
# id is the ntfy topic; deliberately not the entity name, so the tests
# prove binding goes through the id.
id = "alice"
capability = "notifier"
features = ["alert"]
room = "person"

[naming]
en = "Alice's phone"

[write_policy]
mode = "shared"
owner = "ntfy"
