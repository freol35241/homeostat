[package]
name = "homeostat"
version = "0.1.0"
edition = "2021"
description = "A household regulator. Text-first home automation core."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
clap = { version = "4", features = ["derive"] }
