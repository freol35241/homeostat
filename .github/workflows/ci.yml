name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          cache-dependency-glob: |
            sdk/python/pyproject.toml
            adapters/*.py
      - name: Install mosquitto
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends mosquitto
      # First-run dependency resolution must not eat into test timeouts.
      - name: Pre-warm the zigbee2mqtt adapter's uv environment
        run: uv sync --script adapters/zigbee2mqtt.py
      - run: cargo test
